spring.application.name=spring-data-jpa
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.jpa.properties.hibernate.default_schema=student
spring.datasource.username=postgres
spring.datasource.password=12345

spring.jpa.hibernate.ddl-auto=update
# ilk başta create olmalı proje 1 kez çalışınca tablo oluşur. ondan sonra update yap.

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# proje adı
# veritabanı bağlantı bilgileri
# veritabanı şeması (tablosu)
# veritabanı kullanıcı adı
# veritabanı şifresi
# veritabanı tabloları oluşturulurken ne yapılacağı (create, update, validate, none)
# sql sorgularını gösterme 
# sql sorgularını formatla